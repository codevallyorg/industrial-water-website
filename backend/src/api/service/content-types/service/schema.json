{
  "kind": "collectionType",
  "collectionName": "services",
  "info": {
    "singularName": "service",
    "pluralName": "services",
    "displayName": "Service",
    "description": "The 8 service pages. Slug drives /services/<slug>."
  },
  "options": { "draftAndPublish": true },
  "pluginOptions": {},
  "attributes": {
    "title": { "type": "string", "required": true },
    "slug": { "type": "uid", "targetField": "title", "required": true },
    "order": { "type": "integer", "default": 0, "description": "Controls display order in grids, nav and footer." },
    "short": { "type": "text", "required": true, "description": "One-liner on the Home expertise card." },
    "blurb": { "type": "text", "required": true, "description": "Longer card copy on the Services grid." },
    "h1": { "type": "string", "required": true },
    "tagline": { "type": "text", "required": true, "description": "Hero paragraph under the H1." },
    "overview": { "type": "text", "required": true, "description": "The Overview section body." },
    "media": { "type": "component", "repeatable": false, "component": "shared.media-frame" },
    "outcomes": { "type": "component", "repeatable": true, "component": "ui.icon-item", "description": "\"What you get\" - 4 items in the design." },
    "steps": { "type": "component", "repeatable": true, "component": "ui.numbered-item", "description": "\"How it works\" - 4 items in the design." },
    "featurePanel": { "type": "component", "repeatable": false, "component": "ui.feature-panel", "description": "Optional \"Borehole programs\" style block (eyebrow + H2 + image + accent-bar list). Client addition; only groundwater-monitoring uses it." },
    "relatedIndustries": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::industry.industry",
      "description": "ONE-WAY on purpose. Not the inverse of industry.relatedServices - the design's cross-links are asymmetric. Order is curated."
    },
    "seo": { "type": "component", "repeatable": false, "component": "shared.seo" }
  }
}

{
  "collectionName": "components_ui_numbered_items",
  "info": { "displayName": "Numbered Item", "icon": "number-list", "description": "{number,title,description}. Used by: home why, home process, engagement, pillars, service steps." },
  "options": {},
  "attributes": {
    "number": { "type": "string", "required": true, "description": "Ordinal as shown, e.g. 01" },
    "title": { "type": "string", "required": true },
    "description": { "type": "text", "required": true }
  }
}
